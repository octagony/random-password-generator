export interface IState {
  length: number;
  numbers: boolean;
  symbols: boolean;
  lowercase: boolean;
  uppercase: boolean;
}

export interface IPassword {
  id: string;
  name: string;
  value: string;
}
